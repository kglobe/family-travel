# 旅行網頁複用指南

`index.html` 是單一檔案的旅行手冊網頁（手機版介面），下次出國只要換掉裡面的資料就能再用。全部東西都在同一個檔案裡，沒有其他相依檔案。

## 開新旅程前要準備的資料

給 Claude 這些資料，就能直接生出新版網頁：

### 1. 基本資訊
- 旅行名稱（中文 + 英文，例如「大阪親子旅行 / Osaka Family Trip 2026」）
- 日期區間與天數（例如 07.25 六 — 07.30 四 · 6天5夜）
- 出發地與目的地（顯示在頂部路線條，例如「◀ 前鎮 … 難波 ▶」）
- 去程航班：機場代碼、起飛/抵達時間、時區
- 回程航班：同上
- 飯店名稱 + Google Maps 連結 + 訂房 PDF 連結（Google Drive）

### 2. 每日行程（最重要）
每天列出行程點，每個點可以有：

| 欄位 | 說明 | 必填 |
|------|------|------|
| name | 地點或活動名稱 | ✔ |
| time | 時間（只有固定時間的才填，如航班、預約） | |
| type | 分類：`food` 紅 / `attraction` 綠 / `shopping` 金 / `festival` 紫 / `transport` 灰 | ✔ |
| icon | Material Symbols 圖示名（不確定就讓 Claude 挑） | |
| note | 備註：營業時間、交通方式、提醒事項 | |
| map | Google Maps 短連結 | |
| pdf | 預訂文件連結（機票、飯店） | |
| reserve | 餐廳預約資訊（時間、人數、有沒有預定費） | |
| subs | 子項目清單（商場裡的店、園區裡的設施），每項可附地圖連結 | |

蒐集地圖連結的方法：在 Google Maps 找到店家 → 分享 → 複製連結（`maps.app.goo.gl` 短網址）。

### 3. 購物清單
按分類整理（日用品、保健、3C、服飾…），每項可加：
- 品名
- 細節（型號、顏色、規格、比價心得）
- 參考連結（比較文、官網商品頁）

### 4. 行李清單
分「隨身背包」和「行李箱」兩份。網頁有打勾功能，勾選狀態存在手機瀏覽器裡（localStorage），換裝置不會同步。

### 5. 備忘頁
- 行前準備清單（換錢、上網卡、保險、入境卡…）
- 優惠券連結（藥妝、電器行、百貨的免稅折扣頁）
- 行程注意事項（小孩的習慣、逛街優先順序這類經驗談）
- 洗衣排程（帶幾套衣服、哪天洗）
- 花費表 + 記帳 Google Sheet 連結
- 當地緊急電話與就醫資訊

## 要改哪些地方

資料都寫在 `index.html` 的 `<script>` 區塊裡，搜尋這些關鍵字就能找到：

- `const DAYS` — 每日行程（index.html 約 451 行起）
- `const SHOPPING` — 購物清單
- `const PACKING_BACKPACK` / `PACKING_SUITCASE` — 行李清單
- `renderNotes()` — 備忘頁內容（直接寫 HTML）
- `updateCountdown()` — 出發/回程日期時間（倒數計時用）
- `autoSelectDay()` — 旅行期間自動跳到當天的日期對照表
- `<title>`、`.station-name`、`.station-romaji`、`.line-bar` — 頁首標題與日期

主題色和版面不用動。想換主色調再跟 Claude 說。

## 部署

推上 GitHub 後用 GitHub Pages 發布，手機加到主畫面就像 App。
